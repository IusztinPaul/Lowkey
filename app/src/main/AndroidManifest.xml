<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fusionkey.lowkey">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="com.google.android.c2dm.permission.REGISTER" />
    <uses-permission android:name="com.amazonaws.androidtest.permission.C2D_MESSAGE" />

    <application
        android:name=".LowKeyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".entryActivity.EntryActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".main.Main2Activity" />
        <activity android:name=".Chat" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.ConfirmCodeActivity" />
        <activity android:name=".auth.ForgotPasswordActivity" />
        <activity
            android:name=".newsfeed.CommentsActivity"

            android:theme="@style/AppTheme.CommentsActivity" />

        <activity android:name=".ROOMdatabase.MessagesActivity" />
        <activity android:name=".main.menu.profile.EditUserActivity" />
        <activity
            android:name=".main.menu.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".main.menu.profile.ChangePasswordActivity" />

        <service
            android:name="com.amazonaws.mobileconnectors.s3.transferutility.TransferService"
            android:enabled="true" />

        <activity android:name=".auth.LoadUserDataActivity" />
        <activity android:name=".main.menu.about.About" />
        <activity android:name=".main.menu.Menu" />
        <activity android:name=".main.menu.license.LicenseActivity" />

        <service android:name=".pushnotifications.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
             Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_foreground" />
        <!--
             Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"

            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity android:name=".newsfeed.Askaquestion"/>
        <activity android:name=".main.menu.terms.TermsActivity"></activity>
    </application>

</manifest>